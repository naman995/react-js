const JSXRule = () => {
  return (
    <section>
      <h1>JSX Rules</h1>
     <p>JSX must return the single parent element</p>
     <p>JSX element must be properly closed</p>
     <p>JSX element are written using cammelcase (eg, className instead of class)</p>
    </section>
  );
};
export default JSXRule;
